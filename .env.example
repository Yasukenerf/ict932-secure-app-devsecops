# Example environment configuration (DO NOT COMMIT real secrets)

# Flask
FLASK_DEBUG=1
PORT=5000

# Security
SECRET_KEY=change-me-to-a-long-random-string
# Set to 1 only when running behind HTTPS
SESSION_COOKIE_SECURE=0

# Optional: SQLAlchemy database URL (defaults to local SQLite under src/backend/instance)
# DATABASE_URL=sqlite:////absolute/path/to/app.db

# Optional: Flask-Limiter storage (defaults to in-memory)
# RATELIMIT_STORAGE_URI=memory://